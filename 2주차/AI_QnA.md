## FLAB AI Q&A
> Google Meet 녹화 스크립트를 통한 AI요약 질문사항입니다.

### SRP가 무엇인지 설명해보세요.
### 객체란 무엇이라고 생각하나요?
### 객체지향이란 무엇인가요?
### SRP가 왜 필요한가요?
### 응집도가 높은 프로그램이란 무엇인가요?
### OCP(Open/Closed Principle)가 무엇인지 설명해보세요.
### OCP에서 '확장에는 열려 있고 변경에는 닫혀 있다'는 의미가 무엇인가요?
### DI와 IOC의 차이와 각각의 역할은 무엇인가요?
### 결합도가 높으면 왜 안 좋은가요?
### DI가 결합도를 어떻게 낮추는지 설명해보세요.
### 팩토리 패턴이 IOC와 어떤 관련이 있는지 설명해보세요.
### AOP가 무엇이고, 왜 필요한가요?
### 프록시 패턴과 데코레이터 패턴의 차이점은 무엇인가요?
### 스프링 AOP에서 CGLIB와 JDK Dynamic Proxy의 차이점은 무엇인가요?
### 트랜잭셔널 어노테이션이 내부 메서드 호출 시 동작하지 않는 이유를 설명해보세요.
### 디스패쳐 서블릿의 전체 흐름을 코드 기반으로 설명해보세요.

## 내용에 맞춘 질문들
### 오늘 내용에 맞춰진 질문들 SRP(단일 책임 원칙)를 코드 수준에서 어떻게 측정하고, 실제로 위반되는 사례를 리팩토링하는 방법을 설명하세요.
- 응집도와 SRP의 관계를 수치적으로 설명할 수 있는 방법이 있는가?    •  SRP 위반이 시스템 확장성에 미치는 영향은 무엇인가?
- SRP를 지키면서도 성능 저하를 방지하는 설계 방법은?
- SRP와 캡슐화의 차이점과 상호작용은?
- 대규모 시스템에서 SRP를 적용할 때 발생하는 현실적인 트레이드오프는? 

### OCP(개방-폐쇄 원칙)를 스프링 프레임워크에서 실질적으로 구현하는 방법과, 현업에서 적용 시 발생하는 복잡성 문제를 어떻게 해결할 수 있는지 설명하세요.    
- OCP를 적용한 프레임워크와 일반 애플리케이션 코드의 구조적 차이는?    
- OCP 위반이 발생했을 때 유지보수 비용이 증가하는 구체적 사례는?    
- OCP와 DIP(의존성 역전 원칙)의 관계와 차이점은?   
- OCP를 적용하면서 코드 복잡성을 최소화하는 패턴은 무엇인가?    
- 스프링에서 OCP가 적용된 대표적인 예시는 무엇인가? 스프링에서 DI(의존성 주입)와 IOC(제어의 역전)의 차이와, 각각이 코드 구조와 테스트 용이성에 미치는 영향을 설명하세요.
- DI와 IOC의 개념적 차이와 스프링 컨테이너에서의 구현 방식은?
- DI가 결합도를 낮추는 구체적 메커니즘은?    
- IOC가 적용된 시스템에서 객체 생명주기 관리의 장단점은?    
- DI와 팩토리 패턴의 차이점과 유사점은?    
- 테스트 코드 작성 시 DI가 제공하는 주요 이점은? 

### 스프링 AOP에서 JDK Dynamic Proxy와 CGLIB의 차이점, 그리고 각각이 적용되는 조건과 내부 동작 원리를 설명하세요.    
- CGLIB 프록시가 생성 불가능한 경우는 어떤 케이스인가?    
- JDK Dynamic Proxy와 CGLIB의 성능 차이는 어떤 상황에서 두드러지는가?   
- 스프링에서 AOP 적용 시 프록시 선택 로직은 어떻게 동작하는가?    
- 프록시 기반 AOP가 가지는 한계점과 이를 극복하는 방법은?   
- AOP 프록시와 데코레이터 패턴의 근본적 차이점은? 

### 트랜잭셔널 어노테이션이 내부 메서드 호출(self-invocation)에서 동작하지 않는 이유와, 이를 우회하여 트랜잭션을 적용하는 방법을 설명하세요.   
- 트랜잭셔널 어노테이션의 프록시 기반 동작 원리를 설명하라.    
- 스프링에서 트랜잭션 전파(Propagation) 옵션의 의미와 활용법은?    
- 트랜잭션 AOP가 적용되지 않는 대표적인 케이스는?   
- 트랜잭션 경계 설정이 잘못되었을 때 발생할 수 있는 문제점은?    
- 트랜잭션 관리에서 프록시가 아닌 바이트코드 조작 방식의 장단점은? 

### 스프링 디스패쳐 서블릿의 요청~응답 전체 흐름을 코드 레벨에서 설명하고, 각 단계에서 확장 포인트(예: 인터셉터, 핸들러 어댑터 등)를 어떻게 활용할 수 있는지 설명하세요.    
- 핸들러 매핑과 핸들러 어댑터의 역할 분리는 어떤 이점을 제공하는가?    
- 모델앤뷰(ModelAndView) 객체의 생성과 활용 방식은?    
- 스프링 인터셉터와 AOP의 차이점과 적용 시나리오는?   
- 디스패쳐 서블릿의 멀티파트 처리 과정은 어떻게 동작하는가?    
- 커스텀 핸들러 어댑터를 구현할 때 주의해야 할 점은?
