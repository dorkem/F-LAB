# 🗂️ 블루그린 및 로드벨런서 파일 세팅

## 🍴 nginx 설정파일

### 파일 구조

```bash
/etc/nginx/
├── nginx.conf                   ← 기존 파일 그대로 (건드리지 않음)
├── conf.d/
│   └── woowahan-delivery.conf   ← 공통 설정 (server, location 등)
├── blue.conf                    ← 블루 서버 upstream 정의
├── green.conf                   ← 그린 서버 upstream 정의
├── blue-green.conf              ← 심볼릭 링크 (blue or green 가리킴)
├── current                      ← "blue" 또는 "green" 텍스트 저장
└── loadbalancer.conf            ← 로드벨런서 upstream 정의

/usr/local/bin/
└── switch.sh                    ← 트래픽 전환 스크립트
```

### nginx 공통 로직 정의

- `/etc/nginx/conf.d/woowahan-delivery.conf`

```bash
# if blue-green
# include /etc/nginx/blue-green.conf;

# if load-balancer
include /etc/nginx/loadbalancer.conf;

server {
    listen 80;
    server_name woowahan-delivery.site;
    location / {
        root /var/www/woowahan-delivery;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    location /api/ {
        proxy_pass http://app_backend;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection        "";
    }
}
```

<br><br>

## 📚 블루그린일 때

### 1. blue, green.conf

- `blue`: `/etc/nginx/blue.conf`
- `green`: `/etc/nginx/green.conf`

```bash
# blue
upstream app_backend {
    server <블루서버 퍼블릭 IP>;
    keepalive 32;
}

# green
upstream app_backend {
    server <그린서버 퍼블릭 IP>;
    keepalive 32;
}
```

### 2. 심볼릭 링크 초반 설정 및 현재 상태 파일 생성

**심볼릭 링크 생성**

```bash
sudo ln -sf /etc/nginx/blue.conf /etc/nginx/blue-green.conf
```
- `ln` : 링크 파일 만드는 명령어
- `-s` : 심볼릭 링크 (실제 파일 복사가 아니라 가리키는 포인터)
- `-f` : 이미 있으면 덮어씌우기 (force)
- 결과: `blue-green.conf` → `blue.conf` 로 연결됨
- nginx가 `blue-green.conf`를 읽으면 실제로는 `blue.conf` 내용을 읽는 것

<br>

**current 파일 생성**

```bash
echo "blue" | sudo tee /etc/nginx/current
```
- `echo "blue"` : "blue" 텍스트 출력
- `| sudo tee` : 그 출력을 sudo 권한으로 파일에 씀

<br>

**switch.sh 실행권한 및 설정확인 및 적용**

```bash
sudo chmod +x /usr/local/bin/switch.sh

# 문법검사 완료하면 nginx 리로드
sudo nginx -t && sudo nginx -s reload
```

### 3. switch.sh 설정

- `/usr/local/bin/switch.sh`   
- 현재 current 파일 읽어서, 현재가 green이면 blue로, blue면 green으로 변환
- 서버를 끄는게 아니라 시그널을 통해 nginx에게 신호를 보내서 설정을 다시 읽으라고 지시함
  - `nginx -s reload`: `KILL -1(SIGHUP)` 
- nginx는 마스터 프로세스가 `SIGHUP` 신호를 받고 워커 프로세스를 생성해서 실제 요청을 처리하는데 설정을 변경하면 새로운 워커 프로세스가 생성되고, 기존 워커 프로세스는 하던거 끝내고 종료시킴
  - `KILL -3(SIGQUIT)`
 
 
```bash
#!/bin/bash
set -e

CURRENT=$(cat /etc/nginx/current)

if [ "$CURRENT" == "blue" ]; then
    ln -sf /etc/nginx/green.conf /etc/nginx/blue-green.conf
    echo "green" > /etc/nginx/current
    echo "✅ 블루 → 그린 전환 완료"
else
    ln -sf /etc/nginx/blue.conf /etc/nginx/blue-green.conf
    echo "blue" > /etc/nginx/current
    echo "✅ 그린 → 블루 전환 완료"
fi

nginx -t && nginx -s reload
```

### 4. blue-green.conf

- `/etc/nginx/blue-green.conf`

```bash
upstream app_backend {
    server <현재 활성화된 서버 IP>;
    keepalive 32;
}
```

<br><br>

## ⚖️ 로드벨런서일때

- `/etc/nginx/loadbalancer.conf`

```bash
upstream app_backend {
    server <서버1 퍼블릭 IP>;
    server <서버2 퍼블릭 IP>;
    keepalive 32;
}
```


<br><br>

# 📍 PinPoint APM 설치

## 🚨 보안 그룹 설정
- 기존의 엉망이었던 보안그룹을 로드벨런서, 서버, DB, Collector 그룹으로 각각쪼갬

### 로드벨런서 그룹(`sg-lb`)
|포트|허용대상|이유|
|---|---|---|
|22|0.0.0.0/0|ssh 접속|
|80|0.0.0.0/0|사용자 HTTP 요청|
|443|0.0.0.0/0|사용자 HTTPS 요청|


### 백엔드 서버 그룹(`sg-backend`)
|포트|허용대상|이유|
|---|---|---|
|22|0.0.0.0/0|ssh 접속|
|8081|sg-lb|LB에서 오는 요청만 허용|


### Collector 보안그룹 (`sg-pinpoint-collector`)
|포트|허용대상|이유|
|---|---|---|
|22|0.0.0.0/0|ssh 접속|
|8080|내 IP|Pinpoint Web UI|
|9991|sg-backend|Agent 등록|
|9992|sg-backend|Agent 통계|
|9993|sg-backend|Agent 트랜잭션|

### RDS 보안그룹 (`sg-rds`)
|포트|허용대상|이유|
|---|---|---|
|3306|sg-backend|백엔드 서버만 DB 접근 허용|

<br><br>

## 🌎 Collector EC2 생성 및 환경 설정

### EC2 스펙

```bash
인스턴스 타입: t3.medium (최소) - 메모리 4GB 필요
스토리지: 30GB (기본 8GB는 부족)
보안그룹: sg-pinpoint-collector
```

### 스토리지 확장 (8GB → 30GB)

```bash
# AWS 콘솔에서 EBS 볼륨 수정 후 EC2에서 실행
sudo growpart /dev/nvme0n1 1
sudo xfs_growfs /

# 확인
df -h  # 30GB로 늘어나면 성공
```

### 스왑 메모리 설정 (메모리 부족 보완)

```bash
# 스왑이란? RAM이 부족할 때 디스크를 임시 메모리로 사용하는 것
sudo dd if=/dev/zero of=/swapfile bs=128M count=32
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile swap swap defaults 0 0' | sudo tee -a /etc/fstab

# 확인 (Swap: 4.0Gi 보이면 성공)
free -h
```

### Docker 설치

```bash
sudo yum install -y docker
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker ec2-user

# 재로그인 후 docker-compose 설치
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

<br><br>

## Pinpoint 설치 

### Pinpoint란?
```bash
내 서버 내부를 X-ray로 찍어주는 도구

사용자가 /api/stores 호출했을 때
[Nginx] → [Spring Boot] → [DB 쿼리]
  5ms        150ms          200ms

→ 총 355ms, 병목은 DB쿼리! 이런 걸 자동으로 추적
```


### Pinpoint 구성요소
```bash
docker-compose up -d 하면 자동으로 뜨는 것들:

HBase      : 데이터 저장소 (Pinpoint 전용 DB)
Collector  : Agent 데이터 수집
Web UI     : 브라우저로 보는 대시보드 (8080포트)
Zookeeper  : 분산 시스템 조율
Flink      : 실시간 데이터 처리
MySQL      : 메타데이터 저장
Redis      : 캐시
```

### 설치 명령어
```bash
sudo yum install -y git
git clone https://github.com/pinpoint-apm/pinpoint-docker.git
cd pinpoint-docker
docker-compose up -d
```

### 시행착오 1: HBase 테이블 초기화 안됨

- Pinpoint Web UI 접속은 됨
- `Application List`에 `woowahan-delivery`가 안 뜸
- Agent 로그 보니까 연결은 되는데 데이터가 안 쌓임
```bash
# Collector 로그 확인
docker-compose logs pinpoint-collector | tail -20

# 에러 발견
TableNotFoundException: AgentInfo
TableNotFoundException: AgentStatV2
```

```bash
# 1. HBase 컨테이너 안으로 접속
docker exec -it pinpoint-hbase /bin/bash

# 2. 초기화 스크립트 위치 찾기
find / -name "hbase-create.hbase" 2>/dev/null
# → /opt/hbase/hbase-create.hbase

# 3. 스크립트 실행 (테이블 생성)
/opt/hbase/hbase-2.2.6/bin/hbase shell /opt/hbase/hbase-create.hbase

# 4. 테이블 생성됐는지 확인
/opt/hbase/hbase-2.2.6/bin/hbase shell
list
# AgentInfo, AgentStatV2 등 16개 테이블 보이면 성공

# 5. 컨테이너 나가기
exit
exit

# 6. Collector 재시작
docker-compose restart pinpoint-collector
```



## 백엔드 서버에 Agent 설치
### Agent가 뭔지
```bash
내 앱에 붙는 탐정 같은 것
코드 한 줄 안 건드리고 JVM 레벨에서 자동으로 트래픽 추적
```

### 서버1, 서버2 각각 실행
```bash
cd /opt

# Maven Central에서 다운로드 (3.x부터 GitHub 아닌 Maven에서 받아야 함)
sudo wget https://repo1.maven.org/maven2/com/navercorp/pinpoint/pinpoint-agent/3.0.5/pinpoint-agent-3.0.5.tar.gz
sudo tar -xzf pinpoint-agent-3.0.5.tar.gz
sudo mv pinpoint-agent-3.0.5 pinpoint-agent
```

### 시행착오 2: 설정 파일이 여러 개

- pinpoint-root.config에 Collector IP 넣음
- 앱 재시작 했는데 로그 보면 여전히 `127.0.0.1`로 연결 시도


```bash
# 로그에서 이상한 부분 발견
tail -f /opt/pinpoint-agent/logs/server1/pinpoint.log

# 계속 이렇게 나옴
Connection refused: /127.0.0.1:9991  ← 설정했는데 왜 127?

# 설정 파일 확인
grep -n "collector.ip" /opt/pinpoint-agent/pinpoint-root.config
# → <Collector IP> 맞게 들어가 있음

# 다른 설정 파일 있는지 찾기
find /opt/pinpoint-agent -name "*.config" -o -name "*.properties"
# → /opt/pinpoint-agent/profiles/local/pinpoint.config   ← 발견!
# → /opt/pinpoint-agent/profiles/release/pinpoint.config ← 발견!

# 확인해보니
grep "collector.ip" /opt/pinpoint-agent/profiles/release/pinpoint.config
# → profiler.transport.grpc.collector.ip=127.0.0.1  ← 여기가 문제!
```

```bash
# 3개 파일 모두 수정
sudo sed -i 's/profiler.transport.grpc.collector.ip=127.0.0.1/profiler.transport.grpc.collector.ip=<Collector IP>/g' \
  /opt/pinpoint-agent/pinpoint-root.config

sudo sed -i 's/profiler.transport.grpc.collector.ip=127.0.0.1/profiler.transport.grpc.collector.ip=<Collector IP>/g' \
  /opt/pinpoint-agent/profiles/release/pinpoint.config

sudo sed -i 's/profiler.transport.grpc.collector.ip=127.0.0.1/profiler.transport.grpc.collector.ip=<Collector IP>/g' \
  /opt/pinpoint-agent/profiles/local/pinpoint.config

# 확인
grep "collector.ip" /opt/pinpoint-agent/profiles/release/pinpoint.config
# → <Collector IP> 나오면 성공

# 앱 재시작
pkill -f "woowahan-delivery"
sleep 3
./deploy.sh <Collector IP> dev server1
```

### 시행착오 3: 로그 폴더 권한 없음

- 설정 다 맞췄는데 Pinpoint에 앱이 안 뜸
- Agent 로그 파일 자체가 없음

```bash
# 로그 파일 찾기
find /opt/pinpoint-agent -name "*.log"
# → 아무것도 안 나옴 (로그 파일 자체가 없음)

# 앱 로그에서 pinpoint 관련 에러 확인
grep -i pinpoint /home/ec2-user/woowahan-delivery/app.log | head -20

# 에러 발견
ERROR StatusConsoleListener Unable to create file 
/opt/pinpoint-agent/logs/server1//pinpoint.log
```

- `/opt/pinpoint-agent/logs/server1/` 폴더가 없어서 로그 파일을 못 만드는 것.
- Agent가 로그를 못 쓰니까 정상적으로 동작을 안 한 거예요.

```bash
# 폴더 생성
sudo mkdir -p /opt/pinpoint-agent/logs/server1
sudo mkdir -p /opt/pinpoint-agent/logs/server2  # 서버2도

# ec2-user가 쓸 수 있게 권한 변경
sudo chown -R ec2-user:ec2-user /opt/pinpoint-agent/logs

# 확인
ls -la /opt/pinpoint-agent/logs/
# ec2-user 소유로 보이면 성공

# 앱 재시작
pkill -f "woowahan-delivery"
sleep 3
./deploy.sh <SERVER1 IP> dev server1
```


## deploy.sh 수정

### 인자를 하나 더 받도록 수정


## k6 부하 테스트
### k6란?
```bash
가상 사용자를 만들어서 실제로 서버에 요청을 때리는 도구

stages = 몇 명이 테스트할지 스케줄
default function = 각 가상 사용자가 하는 행동

50명이 동시에 /api/stores 계속 호출 → 서버가 버티는지 테스트
```

### k6 설치
```bash
brew install k6
```

### TEST 스크립트
- 수정시 지금은 default로 되어있는데 순차, 다양한 이름으로 테스트 가능
```bash
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  stages: [
    { duration: '1m', target: 50 },   // 1분동안 50명까지 증가
    { duration: '3m', target: 50 },   // 3분동안 50명 유지
    { duration: '1m', target: 100 },  // 1분동안 100명까지 증가
    { duration: '3m', target: 100 },  // 3분동안 100명 유지
    { duration: '1m', target: 0 },    // 종료
  ],
  thresholds: {
    http_req_duration: ['p(95)<500'], // 95%가 500ms 이하여야 함
    http_req_failed: ['rate<0.01'],   // 에러율 1% 미만
  },
};

const BASE_URL = 'http://woowahan-delivery.site';

export function setup() {
  const loginRes = http.post(
    `${BASE_URL}/api/v1/users/auth/login`,
    JSON.stringify({ email: '29034@243', password: '1' }),
    { headers: { 'Content-Type': 'application/json' } }
  );
  const token = loginRes.json('data.accessToken');
  return { token };
}

export default function (data) {
  const headers = {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${data.token}`,
  };

  http.get(`${BASE_URL}/api/v1/stores`, { headers });
  check(http.get(`${BASE_URL}/api/v1/stores?categoryId=6`, { headers }), {
    'status is 200': (r) => r.status === 200,
  });
  http.get(`${BASE_URL}/api/v1/stores/1/menus`, { headers });
  sleep(1);
}
```

### 실행

```bash
k6 run load-test.js
```

### 결과

```bash
Pinpoint Web UI: http://<collector-ip>:8080

확인 가능한 것들:
- Servermap: 트랜잭션 흐름 (USER → 앱 → DB)
- Inspector: CPU, 메모리, TPS 실시간 모니터링
- 응답시간 분포 (1s 이하, 3s 이하 등)
- 어떤 API가 느린지
- DB 쿼리 병목 구간
```

### EC2 껐다 킬 때 순서

```bash
# Collector EC2 켠 후
free -h               # 스왑 확인
sudo swapon /swapfile  # 스왑 없으면 활성화
cd ~/pinpoint-docker
docker-compose up -d  # Pinpoint 실행
# 2~3분 대기 후 브라우저 접속
```

- 콜렉터, 도커, 핀포인트, 스왑메모리, 9991,2,3,4포트 및 에이전트 등록 /통계/ 트랜잭션 각각 설명, pinpoint 구성요소 및 설명, 스크립트 더 자세히
  
