# FLAB AI Q&A
> 📋 FLAB에서 GOOGLE 녹화 음성을 통한 스크립트의 질문사항을 따로 정리하였습니다.

# 🍵 Java
## 어레이 리스트와 링크드 리스트의 차이가 무엇인가요?
- `ArrayList`는 내부가 배열이라 인덱스로 바로 접근할 수 있어서 특정 요소 조회는 O(1)이지만, 중간이나 앞에서 삽입·삭제를 하면 뒤의 요소들을 전부 밀어야 하므로 O(N)이 걸린다.
- `LinkedList`는 노드들이 포인터로 연결된 구조라 인덱스로 바로 접근할 수 없어서 조회는 노드를 하나씩 따라가야 하므로 O(N)이고, 대신 맨 앞이나 맨 뒤에서의 삽입·삭제는 포인터만 바꾸면 되어 O(1)이다(단, 맨 뒤 O(1)은 tail 포인터가 있을 때 기준이다).
- 이런 특성 때문에 스택처럼 뒤에서만 넣고 빼는 구조는 ArrayList로 구현하는 경우가 많고, 큐나 덱처럼 앞과 뒤에서 자주 삽입·삭제가 일어나는 구조는 LinkedList로 구현하는 경우가 많다.

<br><br>

## JVM의 역할과 구조에 대해 설명해 주실 수 있나요?
`JVM`은 OS(운영체제) 위에서 동작한다.<br> 
윈도우, 리눅스, 맥용 JVM이 각각 따로 있으며, <br>
이 구조 덕분에 개발자는 "Write Once, Run Anywhere"라는 말처럼 <br>
OS에 상관없이 동일한 자바 코드를 실행할 수 있다.<br>

### JVM의 내부 구조

1. 클래스 로더 (Class Loader)
    - 작성한 `.class` 파일들을 읽어서 JVM 메모리 영역에 배치한다. (배달원 역할)
2. 런타임 데이터 영역 (Runtime Data Areas):
    - 프로그램을 실행하기 위해 `OS`로부터 할당받은 메모리 공간이다.
    - 우리가 앞서 이야기한 `Stack`과 `Heap`이 바로 이 안에 포함된다.
3. 실행 엔진 (Execution Engine):
    - 메모리에 올라온 바이트코드를 실제로 실행한다.
    - 인터프리터 방식과 성능을 높여주는 JIT 컴파일러 방식을 섞어서 사용한다.
4. 가비지 컬렉터 (Garbage Collector):
    - 힙 영역에서 참조되지 않는 객체들을 찾아 메모리에서 제거한다.

<br><br>

## JIT 컴파일러
- 코드를 한 줄씩 읽으면서 기계어로 즉시 번역(해석)하고 바로 실행하는 `인터프리터` 방식과, 실행 전에 코드 전체를 기계어로 미리 번역하는 `컴파일러` 방식을 섞어서 사용한다.
- 인터프리터 방식으로 실행하다가, 어떤 메서드가 반복적으로 호출하는지 프로파일링하고, 특정 코드가 많이 실행되는 부분은 기계어로 번역해서 캐시로 저장해서 그걸 실행함.

<br><br>

# 🪜 객체지향
## 객체와 객체지향이 뭔지
- `객체`는 우리 주변에서 존재하는 실체를 소프트웨어 개념에다 옮긴 것으로, `상태와 행위`를 가진 하나의 단위이다.
    - 상태: 이름, 나이, 키, 몸무게
    - 행위: 걷기, 뛰기, 말하기
- 물리적인게 아니라 시간과 같은 논리적인 존재도 독립적인 객체로서 존재할 수 있습니다. 따라서 `LocalTime`클래스와 같이 추상적인 시간의 개념을 객체로 만들어 복잡한 로직을 관리할 수 있다.
- `객체지향`은 프로그램 실행이 단순히 위에서 아래로 진행하는 순서가 중요한 절차지향과 달리 객체가 시간의 흐름에 따라서 자신의 상태를 유지하면서 객체끼리 상호작용하며 변화하는 과정

<br>

### 역할, 책임, 협력
1. 역할(인터페이스)
    - 배달원이 남자든 여자든 서울 출신이든 아니든, 배달만 잘 해주면 된다는 틀만 있으면 됨
2. 책임(구현)
    - 오토바이를 타든, 자전거를 타든 어떤 방식으로 수행할지에 대한 구현
3. 협력(클래스간의 호출)
    - 손님은 주문을 하고, 식당은 요리하고 배달원을 호출하고, 배달원은 음식을 손님에게 전달하면 됨.

<br><br>

## 객체지향의 핵심 원칙과 SOLID에 대해 설명해 주세요.

### SRP와 응집도
- 단일 책임의 원칙으로 하나의 클래스는 딱 한 가지 역할만 맡아야 한다는 뜻이다.
- 여러기능이 섞이면 수정할 때마다 불필요한 충돌과 사이드 이펙트(의도하지 않은 결과)가 발생해서 유지보수가 어려워진다.
- SRP를 어기면 메서드가 멤버변수를 부분적으로만 사용하게 되어서 응집도가 떨어지게 된다.
    - 응집도: 클래스 내부의 요소(변수, 메서드)들이 얼마나 밀접하게 서로 연관되어 있는가
    - 연관이 높으면 변경마다 매번 대공사를 거치게 된다.

### 단일 책임 원칙에서 책임이란 무엇일까요?
- 책임이란, '동일한 비즈니스 목적을 가진 기능들의 집합' 입니다.<br>
ex) 계산기 클래스의 `덧셈()`, `뺄셈()`, `곱셉()` 카테고리

<br>

## OCP와 DI, 결합도
- "기존 코드는 건드리지 말고(Closed), 새로운 기능만 추가(Open)할 수 있어야 한다는 뜻이다."
- 예를들면 소나타에 맞춰서 코딩을 했을 때 차를 포르쉐로 바꿔도 정상 동작해야한다.
- 이걸 수행하기 위해서 DI라는 기술이 있는데
- `DI`는 내가 사용할 물건을 직접 생성하지 않고 외부에서 주입해주는 것이다.
- 따라서 운전자는 Car라는 인터페이스만 알고, 외부에서 소나타든, 부가티든 포르쉐를 넣어준다. 그러면 결합도가 낮아지게 된다.
    - 결합도: 한쪽이 변경될 때 다른 쪽이 얼마나 영향을 받는가
    - 결국 결합도가 높으면 소나타에서 부가티로 변경하면 운전자가 일을 못하게 된다.

<br>

### LSP (Liskov Substitution Principle, 리스코프 치환 원칙)
- 상속받은 자식 클래스가 부모의 의도를 망가뜨리면 안된다.
- 예시: List 인터페이스를 구현한 ArrayList를 쓰다가 LinkedList로 바꿔도 코드가 정상 작동해야 합니다. 만약 LinkedList로 바꿨더니 갑자기 에러가 터진다면 LSP 위반이다.

<br>

### ISP (Interface Segregation Principle, 인터페이스 분리 원칙)
- 범용 인터페이스 하나보다 구체적인 여러 개가 낫다.
- 예시: 프린트, 팩스, 스캔 기능을 넣은 프린터 인터페이스를 만들면, 최신 기계는 상관 없지만, 옛날 기계를 구현할 때 안되는 팩스, 스캔까지 모두 구현해야하는 문제가 생긴다.

<br>

### DIP (Dependency Inversion Principle, 의존관계 역전 원칙)
- 의미: 구현 클래스(Concrete Class)를 직접 바라보지 말고, 인터페이스(Interface)를 바라봐야한다.
- Spring의 DI(Dependency Injection)가 이 원칙을 따름

<br><br>

## DI와 IOC의 차이와 각각의 역할은 무엇인가요?
- `IoC`는 흐름의 주도권이 중요한데, 내가 쓰고싶을 때 호출해서 쓰는게 아니라, 프레임워크가 필요할 때 내가 짠 코드를 호출한다는 설계 원칙이다.
- `DI`는 클래스 내부에서 `new`로 직접 생성하는게 아니라, 생성자 등을 통해 외부에서 주입받는 `IoC`를 구현한 방법

<br><br>

## AOP
- `AOP`는 관점 지향 프로그램이라고 하지만, 껴넣기 프로그래밍이라고도 한다.
- 매 클래스의 매 매서드에 시간측정을 한다고 가정했을 때, 파일이 늘어나고, 메서드가 늘어나면 코드가 지저분해지기 때문에 사용한다.
- `CGLIB`은 원본 클래스를 상속해서 가짜 프록시를 만들어서, final 클래스는 상속이 안되어 AOP를 못하고, 내부 호출이 있으면 실제 객체가 호출되어 무시되는 경향이 있다
- 반면에 `JDK 다이나믹 프록시`는 리플렉션을 사용하여 인터페이스를 구현한 프록시 객체가 생성된다. 인터페이스가 필수고, 인터페이스에 정의된 메서드만 프록시로 사용이 가능하다.

<br><br>

## 트랜잭셔널 AOP가 무엇이고, 트랜잭셔널 어노테이션이 내부적으로 어떻게 동작하는지 설명해 주세요.
- 트랜잭션을 처리할 때 AOP가 없으면, 모든 서비스 메소드마다`try-catch` 문을 안에서 `DB 커넥션`을 가져와 `commit()`이나 `rollback()`을 호출해야 합니다.
- Spring이 `@Transactional`이 붙은 `빈(Bean)`을 발견하면, 실제 객체 대신 이를 감싸는 `프록시 객체`를 생성하여 빈 컨테이너에 등록합니다.
    1. 해당 메서드 실행하면 프록시가 가로채서 `Transaction Advisor`으로 트랜잭션 시작
    2. 내부적으로는 `TransactionManager`가 `TransactionSynchronizationManager`(스레드로컬)라는 클래스를 통해 커넥션을 가져온다
    3. 오토커밋을 false로 한다.
    4. 비즈니스 로직 처리한다.
    5. 성공시 커밋, 실패시 롤백
    6. 자원반납

<br><br>

# ⚙️ OS
## 쓰레드와 프로세스의 차이는 무엇인가요?
- 프로세스는 실행중인 프로그램이고, 쓰레드는 프로세스 내에서 실제 코드를 실행하는 흐름이다.
- 프로세스는 프로세스 별로 독립된 메모리 영역(Code, Data, Stack, Heap)을 가지고, 프로세스간 통신을 위해서는 IPC라는게 필요하다.
- 그에 비해 쓰레드는 Code, Data, Heap 영역을 공유해서 여러 작업을 번갈아 실행하는 동시성과, 한번에 여러 작업을 처리하는 병렬성을 보장해서 성능을 높인다.

### IPC
- 서로다른 프로세스들이 데이터를 주고받기 위해 사용하는 통로이다.
- OS가 보안과 안정성을 위해 각 프로세스마다 메모리를 할당해서 침범이 불가능하지만, 개발할 때는 필요해서 만듦
- 공유 메모리를 통해 두 프로세스의 공용 메모리 공간을 만들어 준다던지, OS가 두 프로세스 사이에서 편지를 대신 전달해주는 메시지 패싱방법등이 있다.
- 각기 다른 프로세스는 언어나 메모리 주소 체계나 위치등이 다 다른데, 이걸 직접적으로 소통할 수 없어서 `인터페이스`로 이런 규칙으로 대화하자고 정해둠

<br><br>


## 쓰레드
- 여러 스레드가 Heap 영역이나 데이터 영역의 공유 자원(임계영역)에 동시에 접근해서 데이터를 수정할 때, 값이 꼬일 수 있다.
- 뮤텍스와 세마포어
    - 운영체제에서 자원에 접근할 수 있는 열쇠를 관리하는 방식
    - 뮤텍스: Mutual Exclusion(상호 배제)의 줄임말로 1명이 열쇠를 들고 들어가서 문을 잠그고, 나올때 열쇠를 반납한다.
    - 세마포어:
- volatile: CPU 캐시가 아니라 메인 메모리에서 직접 데이터를 읽고 쓰도록 강제하는 키워드
- CAS(=낙관적 락, 아토믹): 데이터를 읽기 전부터 누가 건드릴 수도 있으니 일단 문부터 잠그는 일반적인 락(비관적 락)과 다르게, 건드리지 않을 것이라 생각하고 문을 잠그지않고 계산한 후에, 마지막 데이터를 저장하기 전에 변경되지 않았는지 확인하는 방법( 만약에 누가 건드렸으면 수정취소됨 )
    - ConcurrentHashMap: 일반적인 Hashtable은 싱크로나이즈를 붙여서 뮤텍스락을 사용하니 누가 데이터를 하나만 고쳐도, 전체 데이터를 다 잠궈버리지만, 얘는 구역별로 나눠서 만지는 부분만 잠근다. 해시맵은 암것도 안써서 멀티스레드에서 사용하지 못함


<br><br>

## 스택과 힙의 차이점은 무엇인가요?
### 스택 (Stack): 정적 메모리 할당
- 용도: 함수 호출 시 사용되는 지역 변수, 매개변수 등이 저장된다.
- 구조: `LIFO(Last In, First Out)` 구조로, 함수가 끝나면 바로 메모리에서 사라진다.
- 컴파일 시점에 크기가 결정된다.

### 힙 (Heap): 동적 메모리 할당
- 용도: `new` 키워드로 생성되는 객체나 배열 같은 참조형 데이터가 저장된다.
- 구조: 개발자가 직접 참조를 끊어서 메모리 공간을 확보하거나, 가비지 컬렉터(GC)가 관리한다.
- 런타임(프로그램 실행 중)에 크기가 결정된다. 

<br><br>

# 🕸️ 네트워크

## TCP/IP (전송 제어 프로토콜)
- TCP/IP는 인터넷에서 데이터를 안정적으로 보내기 위한 전체적인 규칙의 모음입니다. 단순히 연결뿐만 아니라 데이터를 쪼개고, 보내고, 확인하는 전 과정을 포괄합니다.
- 커넥션 식별 (5-Tuple): 연결을 고유하게 식별하기 위해 다음 5가지 정보가 일치해야 합니다.
    - 출발지 IP / 출발지 포트 번호 / 목적지 IP / 목적지 포트 번호 /프로토콜 (TCP)
- 주요 장점:
    - 신뢰성(Reliable): 데이터 유실 시 재전송을 요청합니다.
    - 순서 보장: 데이터가 뒤섞여 도착해도 원래 순서대로 정렬합니다.
    - 제어 기능: 흐름 제어(받는 쪽 속도에 맞춤), 혼잡 제어(네트워크 상태에 맞춤)를 수행합니다.
    - 양방향 통신: 클라이언트와 서버가 동시에 데이터를 주고받을 수 있습니다.

<br><br>

## 3-Way Handshake (연결 수립 과정)
- TCP의 특징인 '신뢰성'을 확보하기 위해, 실제 데이터를 보내기 전 상대방과 통신 준비가 되었는지 세 번 주고받으며 확인하는 구체적인 단계입니다.

### 진행 절차
1. [SYN] 단계: 클라이언트가 서버에게 "통신 시작할까? 내 번호는 이거야(Sequence Number)"라고 요청을 보냅니다.
2. [SYN + ACK] 단계: 서버가 요청을 확인하고 "응, 나도 준비됐어! 내 번호는 이거고, 네 번호 잘 받았어(Acknowledgment Number)"라고 답합니다.
3. [ACK] 단계: 클라이언트가 마지막으로 "확인 완료! 이제 진짜 데이터 보낼게"라고 답하며 연결이 최종 성립됩니다.

### 특징 및 단점
- 지연 시간(Latency): 데이터를 보내기도 전에 최소 3번의 왕복이 필요하여 물리적인 시간이 소요됩니다.
- 보안 취약점 (SYN Flooding): 공격자가 SYN만 계속 보내고 마지막 ACK를 보내지 않아 서버의 연결 리소스를 꽉 채워버리는 공격에 노출될 수 있습니다.

<br><br>

## 포웨이 핸드셰이킹은 언제 사용되며, 그 과정에서 발생할 수 있는 문제점은 무엇인가요?
- 4-way-handshake은 연결종료시에 사용된다.
1. `FIN`: 클라이언트가 이제 보낼거 다 보냄
2. `ACK`: 서버가 일단 확인했고, 이제 보낼 게 남았으니 조금 기다리라는 Half-Close상태가 됨
3. `FIN`: 서버가 남은 데이터를 다 보내고 끊자는 의미로 보냄
4. `ACK`: 클라이언트도 확인함
- `TIME_WAIT`이라고 클라이언트가 마지막 ACK을 보내고 지연패킷이 올 수 있으므로 기다리게 되면서 잠깐 점유하게 된다. (자원낭비)
- 보통 2분 정도 기다림

<br>

### `TIME_WAIT` 상태가 필요한 이유
- 클라이언트가 서버에게 `ACK`을 보냈지만 중간에 유실되면 서버는 계속 `FIN`을 보내게 된다. `TIME_WAIT`상태 덕분에 `ACK`이 안갔다는 사실을 알 수 있게된다.
- `ACK`을 보내고 닫았다가 나중에 새 연결을 할 때 떠돌던 패킷이 갑자기 도착해버려서 데이터가 오염됨
  
<br><br>

## TCP와 UDP의 차이점은 무엇인가요?
- `UDP`는 데이터를 그냥 던지고, 확인하지 않아 빠르다.(게임, 스트리밍등)
- `TCP`는 3way-handshake연결을 통해 안전한 연결을 하고, 순서가 바뀌거나 누락되면 다시 보내게된다. (이메일, 파일전송 등)

<br><br>

## HTTP 1.0의 Keep-Alive와 HTTP 1.1의 지속 커넥션의 차이점은 무엇인가요?
- `HTTP 1.0`의 `Keep-Alive`는 비표준 기능이라서 `멍청한 프록시`가 헤더를 서버에 전달만 하고 자신은 연결을 끊어서 통신이 잘 되지 않았다.
- `HTTP 1.1`에서는 지속 커넥션을 규약으로 표준화해서 전체 구간에서 지속적으로 연결을 유지하게 만들었다.

<br><br>

## HOL(Head-of-Line Blocking)
- 앞에있는 네트워크 패킷이 전송되지 못하고 있어서 뒤에 있는 패킷들 전체의 전송이 지연되는 것

### HTTP/1.1
- HTTP/1.1은 기본적으로 요청 하나를 보내면 응답이 올 때까지 기다려야 했습니다. 이를 극복하려고 `파이프라이닝(Pipelining)`을 도입했죠.
    - 파이프라이닝: 응답을 기다리지 않고, 요청을 여러개 연속으로 보내는 것
    - 요청은 한번에 여러개 보내도, 서버는 받은 순서대로 응답해야되니까, 첫 번째 데이터가 너무 크면 뒤에 가벼운 데이터들까지 느려지는 `HTTP`레벨의 HOL 발생

## HTTP/2
- HTTP레벨의 HOL 문제를 해결하기 위해 데이터를 작은 프레임 단위로 쪼개서 무작위로 보냄(멀티플렉싱 `Multiplexing`)
- 하지만 TCP 프로토콜로 전송하다보니, 1번의 조각이 모두 다 올때까지 2번이후로부터 다 기다리는 `TCP 레벨의 HOL`에 걸림

## HTTP/3
- 위의`TCP 레벨의 HOL`을 해결하기 위해 구글에서 QUIC이라는 UDP기반으로 여러개의 데이터를 보낼 때 독립적인 통로로 보내서 통과하면 브라우저(애플리케이션)으로 올리고, 빠진 데이터는 다시 요청하게 된다.

<br><br>

## TCP의 성능과 관련된 주요 이슈와 이를 해결하기 위한 알고리즘에는 어떤 것들이 있나요?
### 성능이슈
1. 지연 시간: 데이터를 보내기 전에 3-way-handshake를 해야하므로 연결하는데 오래걸린다.
2. 혼잡: 네트워크 통로가 좁아지면 패킷을 잃어버리고, 재전송하면서 속도가 느려진다.
3. HOL Blocking: 패킷 하나가 유실되면 그 다음 패킷들이 잘 도착해도 애플리케이션에 전달되지 못하고 기다려야한다.

<br>


### 혼잡제어
- 네트워크 내에 전송하는 패킷의 수가 너무 많아져서 전송속도가 늦어지는걸 막음
- Slow Start: 처음에는 조금씩 보내면서 성공의 의미로 ACK을 받으면 2배씩 늘리면서 보냄

### 흐름제어 
- 송신자와 수신자의 메모리나 버퍼의 사이즈를 보고 여유가 있는지 없는지를 보고 윈도우 사이즈를 조절하거나, ack이 올때까지 무조건 멈춰서 기다림

<br><br>

## 슬로우 스타트와 네이글 알고리즘의 차이점은 무엇인가요?
### 슬로우 스타트
- 한 번에 많이 보내서 네트워크가 터지는, 네트워크 혼잡 방지를 목적으로 한다.
- 초기 전송속도가 느리다.

### 네이글 알고리즘
- 작은 패킷을 너무 많이 보내서 대역폭이 낭비되는, 네트워크 대역폭 낭비를 방지하는 목적으로 한다.
- ACK을 기다리는데 지연시간이 발생한다.

<br><br>

### 디스패처 서블릿의 동작 흐름을 설명해보세요.
DispatcherServlet(요청 받음) > HandlerMapping(처리할 컨트롤러를 찾음) > HandlerAdapter(컨트롤러 실행 도구를 찾음) > Controller(비즈니스 로직 수행) > ViewResolver(화면 준비) > DispatcherServlet(결과물 받음) > HTTP 응답

<br><br>

# 🧺 DB
## 트랜잭션이란 무엇인가요?
여러 가지 일을 '하나의 덩어리'로 묶는 것으로, 무조건 다 같이 성공하거나 다 같이 취소되어야 합니다.

<br><br>

## 트랜잭션 동기화란 무엇인가요?
- 과거에는 DB 작업을 위해 Connection 객체를 모든 메서드에 파라미터로 전달해야 했는데, 코드가 너무 지저분해지므로, 커넥션 객체를 `ThreadLocal`에 넣어두고, 커넥션이 필요할 때마다 꺼내쓰도록 만든것

<br><br>

## 트랜잭션의 ACID 원칙에 대해 설명해 주세요.
### Atomicity (원자성) - "All or Nothing"
- 트랜잭션 내의 작업은 모두 성공하거나, 아니면 아예 없던 일처럼 실패해야한다.
- A의 잔고에서 1만 원을 뺐는데, B의 계좌에 넣는 과정에서 에러가 났으면 A의 돈도 원상복구 되어야한다.

### Consistency (일관성) - "법칙 준수"
- '잔고는 0원 미만이 될 수 없다'는 제약조건이 있다면, 잔고가 부족한 사람의 송금 트랜잭션은 아예 거부되어야 한다.
- 트랜잭션이 성공적으로 끝나면, 데이터베이스는 언제나 일관된 상태(규칙이 지켜진 상태)여야 한다.

### Isolation (격리성) - "끼어들기 금지"
- 여러 트랜잭션이 동시에 실행될 때, 서로의 작업에 간섭하면 안된다. 마치 혼자 실행되는 것처럼 격리되어야 한다.
- A가 통장 잔고를 조회하고 있는 도중에, B가 그 통장의 돈을 출금해버려서 A가 엉뚱한 잔액을 보게 만들면 안된다.

### Durability (지속성) - "영구 보존"
- 트랜잭션이 성공적으로 완료(Commit)되었다면, 그 결과는 영구적으로 반영되어야 한다.

<br><br>

## MVCC와 언두 로그의 상관관계에 대해 설명해 주세요.
- 이전에는 락이 걸려있으면 쓰는 중에 읽는게 불가능해 지연이 많이 발생했다. 따라서 `MVCC`는 `Undo 로그`를 이용해 쓰기 중에도 읽기를 허용함으로써, 락(Lock) 없이 데이터의 과거 버전을 조회하게 만드는 기술이다.

<br><br>

## 언두 로그(undo log)가 무엇이며 언제 사용되는지 설명해 주세요.
- 실행 취소시: 데이터 변경시 이전 값을 언두로그에 기록하기 때문에, 쿼리 실행 중 에러가 발생하거나, `Rollback`시 사용된다.
- MVCC에서 과거 버전 보여줄 때: 트랜잭션이 데이터를 수정중일 때, 다른 사용자가 데이터를 조회하면 수정전의 데이터를 보여줌
- 장애 복구시: 시스템이 `COMMIT`이전에 종료되면 다시 시작될 때, Redo를 통해 마지막 상태를 만들고, 커밋되지 않은 언두 로그를 참조하여 모두 롤백시킨다.


<br><br>

## 트랜잭션 격리 레벨 4가지를 설명하고 각 레벨에서 발생할 수 있는 문제를 말씀해 주세요.
1. `READ UNCOMMITTED` (커밋되지 않은 읽기)
    - 다른 트랜잭션이 수정한 후 아직 커밋하지 않은 데이터도 읽을 수 있다.
    - `Dirty Read`가 발생할 수 있다.
    - A가 데이터를 수정하고 커밋하지 않았는데 B가 그 값을 읽음. 만약 A가 롤백하면 B는 존재하지도 않는 가짜 데이터를 들고 있게 됨.
2. `READ COMMITTED` (커밋된 읽기)
    - 커밋이 완료된 데이터만 읽을 수 있습니다. Oracle의 기본값이며, `MVCC`(Undo 로그)를 사용해 커밋 전에는 이전 값을 보여준다.
    - `Non-Repeatable Read`가 발생할 수 있다.
    - 한 트랜잭션 내에서 같은 쿼리를 두 번 실행했는데, 그 사이에 다른 트랜잭션이 값을 수정/커밋하면 두 번의 조회 결과가 다르게 나타남.
3. `REPEATABLE READ` (일관된 읽기)
    - 자신의 트랜잭션이 시작되기 전에 커밋된 내용만 읽습니다. MySQL InnoDB의 기본값입니다.
    - InnoDB가 순차적으로 증가하는 트랜잭션 고유 번호를 비교해 자신보다 늦은 번호의 데이터는 Undo 로그를 통해 내 번호보다 이전 데이터를 조회한다.
    - `Phantom Read`가 발생할 수 있다.
    - InnoDB의 로우 락은 실존하는 행만 잠궈 '빈 공간'을 방어하지 못하며, 새로 삽입된 행은 '이전 기록(Undo)'이 존재하지 않아 필터링되지 않고 유령처럼 나타난다.
    - 그래서 갭락(빈 공간을 잠금), 넥스트 키 락(레코드+갭 락)을 사용해서 빈 공간을 다 잠궈 거의 발생하지 않는다.
4. `SERIALIZABLE` (직렬화 가능)
    - 제일 강한 격리 수준입니다. 읽기 작업에도 공유 잠금(Shared Lock)을 걸어, 다른 트랜잭션이 해당 데이터를 절대 수정하거나 삽입할 수 없게 만듭니다.
    - 느리다.

<br><br>

## DB 인덱스의 자료구조와 동작 원리를 설명해 주세요.
1. 인덱스가 없으면 테이블 풀 스캔을 때리기 때문에 데이터가 늘어날 수록 조회가 느려진다.
2. 그래서 B(Balanced)-Tree를 도입해서 항상 정렬된 상태로 유지해서 값을 찾을때 풀스캔(O(N)을 하는 대신에, O(logN)의 속도로 데이터를 찾을 수 있다.
3. 기수성(카디널리티)이 높은 값을 찾아야 한다. (기수성이 높으면 값의 종류가 많고 중복이 적음) ex) 주민등록번호, ID, email등
4. CUD는 매번 정렬을 해야해서 R을 위해 희생시키는 구조임
5. 인덱스가 너무 많으면 옵티마이저가 어떤 인덱스를 쓸지 고민하고, 실행계획을 세우느라 시간이 많이걸린다.
6. 따라서 조건절에 자주 등장하거나, 데이터 수정보다 조회가 많고, 데이터 규모가 클 때 사용해야한다.

- 조건절로 조회하면 DB는 테이블 대신 B-Tree 인덱스를 먼저 보고, 루트 노드에서 해당 위치의 리프 노드 주소를 찾아가 결과를 반환함

### 이진트리와 B-Tree의 차이
- `이진트리`는 자식 노드가 최대 2개, `B-Tree`는 M개
- `이진트리`는 자식 노드가 추가되면 위 아래로 길어지고(데이터가 , `B-Tree`는 양옆으로 뚱뚱해짐

<br><br>

## 클러스터드 인덱스
- 클러스터드 인덱스는 프라이머리 키(PK)를 기준으로 값이 가까운 레코드들을 같은 데이터 페이지에 묶어 저장하고, 인덱스의 리프 노드에 실제 레코드가 들어 있는 구조임
  - InnoDB는 데이터 페이지 주소 대신 레코드 자체를 클러스터드 인덱스 리프에 저장함으로써 불필요한 추가 I/O를 줄인다. 
- 클러스터드 인덱스에서 PK 변경은 단순 업데이트가 아니라 데이터 재배치와 전체 인덱스 갱신을 동반하는 고비용 작업이다.

### PK가 없을 때
1. PK가 있으면 기본적으로 프라이머리 키 기준
2. Not Null 옵션의 유니크 인덱스 중 첫번째 인덱스를 클러스터링 키로 선택
3. 그거 마저 없으면 자동으로 유니크한 값을 가지도록 증가하는 칼럼을 내부적으로 추가함

### 클러스터드 인덱스의 장단점
- **`장점`**
  - PK로 검색할 때 처리 성능이 빠르다. (특히 범위 검색이 빠름)
  - 커버링 인덱스로 바로 처리가능
- **`단점`**
  - 클러스터링 키 값이 클수록 전체적으로 인덱스가 커짐(세컨더리, 프라이머리키)
  - 세컨더리를 통해 PK를 한 번 더 조회해야해서 성능이 느림
  - Insert시에 프라이머리 키에 의해 레코드 저장 위치가 결정되어서 느림(수정시에도 삭제 삽입이라 느림)

<br><br>

## 정규화를 하는 이유
### 이상현상을 해결하기 위해
- 삽입이상: 데이터를 삽입하기 위해 불필요한 정보까지 함께 입력해야 하는 상황
    - 학생이 수강 신청을 안 하면 학생 정보 자체를 등록하지 못함.
- 갱신 이상: 중복된 데이터 중 일부만 수정되서 데이터 간 불일치가 발생하는 상황
    - 사용자의 주소가 바뀌었는데, 10개의 주문 기록 중 5개만 수정됨.
- 삭제 이상: 정보를 삭제할 때 유지되어야 할 다른 정보까지 연쇄적으로 삭제되는 상황.
    - 강의 수강 기록을 지웠는데 학생의 개인정보까지 같이 날아감.

### 정규화 
1. 제1정규형: 모든 칸에는 하나의 값만 들어가야한다.
2. 제2정규형: 기본키의 일부분에만 의존하는 열이 없어야한다.(부분 함수 종속 제거)
3. 제3정규형: 기본키가 아닌 열끼리 의존하는 관계가 있어서는 안된다.(A->B, B->C라는 이행 종속을 제거해야한다.)

<br><br>

## 트랜잭션 어노테이션이 JPA, MyBatis 등에서 변경 없이 동작 가능한 이유는 무엇인가요?
- 스프링이 트랜잭션 추상화를 제공하기 때문이다.
- 어떤 DB가 들어오든 `PlatformTransactionManager`라는 약속이 있다.
- 어떤 기술이든 트랜잭션 시작은 `getTransaction()`, 커밋은 `commit()`, 롤백은 `rollback()`
- 그러면 JPA는 엔티티매니저 쓰고, 마이바티스는 커넥션 씀

<br><br>

## 데드락 상황
- 요리사 A와 B가 있고, 주방에는 `칼`과 `도마`가 딱 하나씩만 있음
1. 요리사 A가 칼을 먼저 집었습니다. (Lock 1)
2. 그사이 요리사 B가 도마를 집었습니다. (Lock 2)
3. 요리사 A는 이제 도마가 필요한데, B가 들고 있어서 기다립니다.
4. 요리사 B는 이제 칼이 필요한데, A가 들고 있어서 기다립니다.
- 결과: 두 요리사는 서로의 도구를 영원히 기다리는데 이게 교착상태이다.

<br>

### 데드락의 조건
- 아래의 4개가 동시에 만족되어야함
1. 상호 배제: 자원은 한 번에 한 프로세스만 이용할 수 있다. ( 칼 하나를 두명이서 잡고 썰 수 없음 )
2. 점유와 대기: 자원을 가진 상태에서 다른 자원을 기다린다. ( A는 칼든 채로 도마가 나오길 기다린다. )
3. 비선점: 남이 가진 자원을 강제로 뺏을 수 없다. ( A가 B의 도마를 뺏지못함 )
4. 환형대기: 대기 관계가 원을 이룬다. ( A는 B를 기다리고 B는 A를 기다림 )

<br><br>


## DB 락에서 리드락과 라이트락의 차이점은 무엇인가요?
- 리드락: 내가 읽을 때, 남이와서 읽는건 되지만 쓰는건 안된다.
- 쓰기락: 내가 수정하거나 쓰고있을 때, 남은 읽거나 쓰는게 불가능하다.

<br><br>

## OSI 7계층이 왜 존재하는지 설명해 주세요.

## JPA에서 연관관계의 주인이란 무엇인가요?
## JPA에서 Enum 타입을 사용할 때 주의해야 할 점은 무엇인가요?
- `EnumType.ORDINAL`을 쓰지 말것: 기본값으로 `Enum`의 순서(0, 1, 2...)를 DB에 저장합니다. 나중에 Enum 사이에 새로운 값이 추가되면 기존 데이터의 의미가 완전히 뒤섞여버리는 대참사가 일어납니다.
- `EnumType.STRING`을 써야함: `Enum`의 이름을 문자열 그대로 저장합니다. 데이터 크기는 조금 더 커지지만, 변경에 안전하고 가독성이 훨씬 좋습니다.


## GC구역별 정리
- 금방 죽을 객체(Young)는 작은 구역에서 자주 빨리 치우고, 오래 버티는 객체(Old)는 큰 구역으로 보내서 가끔 꼼꼼히 치우는 전략입니다.
- 메모리 구역의 구분과 GC의 일반적인 동작흐름으로 Eden -> Minor GC -> 서바이벌 -> 생종경쟁후 승격 -> 메이저 GC의 과정 공부
- 버전별로 다르지만 일반적으로 설명이 가능한 것들로 공부
    - Parallel GC, G1 GC, ZGC등

## JPA에서 OneToMany 관계를 사용하지 않고 ID 기반으로 설계할 때의 차이점과 그 이유를 설명해보세요.
## ORM이 해결하고자 하는 패러다임 불일치란 무엇인지 설명해보세요.

## 자바 스트림(Stream)의 특징과 주의해야 할 점을 설명해보세요.

